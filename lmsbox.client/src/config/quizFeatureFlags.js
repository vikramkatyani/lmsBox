/**
 * Toggle quiz behaviour without removing routes, APIs, or database fields.
 * Set enableCriticalSafetyQuestions to true when re-enabling the feature.
 */
export const quizFeatureFlags = {
  enableCriticalSafetyQuestions: false,
};
